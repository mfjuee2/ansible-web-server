---
- name: Install Redis
  apt:
    name: redis-server
    state: present

- name: Deploy Redis Configuration
  template:
    src: redis.conf.j2
    dest: /etc/redis/redis.conf
    owner: redis
    group: redis
    mode: '0644'
  notify: restart redis